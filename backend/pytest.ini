[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
; Override pytest's default norecursedirs (which skips dirs named "build"/"dist")
; so the mirrored tests/unit/.../build package is collected.
norecursedirs = .* __pycache__ node_modules *.egg
filterwarnings =
    ignore::DeprecationWarning
