[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
; tests are plain functions; without this pytest tries to collect domain
; classes named Test* (TestCase, TestResult) and warns on every run
python_classes =
